/*
import {Component} from '@angular/core';
import {SonicManager} from '../../game/sonicManager';

@Component({
    selector: 'tile-piece',
    templateUrl: 'app/layout/tilePieceLayout/tilePieceLayout.html'
})
export class TilePieceLayout {
    tilePieces: string[];

    ngOnInit() {
        this.tilePieces = SonicManager.instance.sonicLevel.tilePieces.map(a => a.getImage().canvas.toDataURL());
    }
}
*/
