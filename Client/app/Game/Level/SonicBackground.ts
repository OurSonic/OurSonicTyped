import {Point} from "../../Common/Utils";

export class SonicBackground {
    public Width: number;
    public Height: number;
    public Draw(canvas: CanvasRenderingContext2D, point: Point, wOffset: number): void {

    }
}