export enum GameState {
    Playing,Editing
}

export enum ClickState {
    Dragging,
    PlaceChunk,
    PlaceRing,
    PlaceObject
}
export enum ChunkLayer {
    Low = 0,
    High = 1
}

export enum RotationMode {
    Floor = 134,
    RightWall = 224,
    Ceiling = 314,
    LeftWall = 44
}